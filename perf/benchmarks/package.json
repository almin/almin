{
  "private": true,
  "name": "benchmarks",
  "version": "1.0.0",
  "description": "compare almin versions",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "almin": "file:../../",
    "almin-0.9": "file:pacakges/almin-0.9"
  },
  "devDependencies": {
    "babel-loader": "^6.3.2",
    "babel-polyfill": "^6.23.0",
    "babel-preset-es2015": "^6.22.0",
    "benchmark": "^2.1.3",
    "cross-env": "^4.0.0",
    "lodash": "^4.17.4",
    "mocha": "^3.2.0",
    "testem": "^1.15.0",
    "webpack": "^2.2.1",
    "rm-local-modules": "0.0.1"
  },
  "scripts": {
    "prepare": "rm-local-modules && npm i",
    "build": "webpack",
    "bench": "npm run prepare && cross-env NODE_ENV=production node index.js",
    "test": "npm run prepare && cross-env NODE_ENV=production mocha",
    "test:browser": "npm run prepare && testem ci"
  },
  "author": "azu",
  "license": "MIT"
}
